/**
 * Route entry point for /dashboard.
 * Re-exports the ContributorDashboard component so the router
 * can lazy-load a single chunk for this route.
 * @module pages/DashboardPage
 */
export { default } from '../components/dashboard/ContributorDashboard';
