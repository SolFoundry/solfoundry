/**
 * useAuth — convenience hook that surfaces the AuthContext value.
 * Provides isAuthenticated, user, login, logout, updateUser.
 */
export { useAuthContext as useAuth } from '../contexts/AuthContext';
