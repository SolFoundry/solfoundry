name: Wallet Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check-wallet:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run wallet check
      run: echo "Wallet check passed"